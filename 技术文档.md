# 超星学习通自动刷课工具 - 技术文档

> 版本 v3.1.3 | Python 3.10+ | 基于 Samueli924/chaoxing 二次开发

## 一、系统概述

本工具用于自动化完成超星学习通平台的课程任务，包括视频播放、文档阅读、阅读任务和章节测验答题。章节测验通过 DeepSeek 大模型自动生成答案并提交。

**核心能力：**
- 自动登录（AES-CBC 密码加密）
- 课程章节遍历与任务点处理
- 视频/音频播放进度模拟（MD5 加密上报）
- 章节测验 LLM 答题（单选/多选/填空/判断/简答）
- 题库答案缓存（本地 JSON）
- 未开放章节回滚重试
- GitHub Actions 定时/手动触发运行

---

## 二、技术栈

| 组件 | 技术 |
|------|------|
| 语言 | Python 3.10+ |
| HTTP 客户端 | `requests`（Cookie 持久化、连接池） |
| HTML 解析 | `BeautifulSoup4` + `lxml` |
| 加密 | `pyaes`（AES-256-CBC） + `hashlib`（MD5） |
| 大模型 | DeepSeek API（`deepseek-chat` → v4-flash），极简 token 优化 |
| 日志 | `loguru` |
| 验证码 | `ddddocr`（带带弟弟 OCR） |
| 字体解码 | `fonttools` |
| 部署 | GitHub Actions / Docker |

---

## 三、项目结构

```
SuperStar-main/
├── main.py                  # 程序入口，CLI 参数解析，主控制流
├── config.ini               # 运行时配置（gitignore 保护）
├── config_template.ini      # 配置文件模板
├── requirements.txt         # Python 依赖
├── Dockerfile               # Docker 容器定义
├── .github/workflows/       # GitHub Actions 工作流
│   └── main.yml
├── api/
│   ├── base.py              # 超星平台 API 封装（核心业务逻辑）
│   ├── answer.py            # 题库插件系统（Tiku 基类 + 7 种实现）
│   ├── answer_check.py      # 答案格式校验
│   ├── decode.py            # HTML 页面解析（课程/章节/题目）
│   ├── cipher.py            # AES-CBC 加密（登录用）
│   ├── config.py            # 全局常量（AES Key、UA、Headers）
│   ├── cookies.py           # Cookie 持久化读写
│   ├── captcha.py           # 验证码自动识别
│   ├── cxsecret_font.py     # 超星加密字体解析
│   ├── font_decoder.py      # 字体解密映射
│   ├── logger.py            # loguru 日志配置
│   ├── exceptions.py        # 自定义异常类
│   ├── notification.py      # 外部通知（Server酱/Qmsg/Bark）
│   ├── process.py           # 终端进度条显示
│   └── __init__.py
└── resource/
    └── font_map_table.json  # 字体字形映射表（3 万行）
```

---

## 四、核心流程

### 4.1 主控制流（main.py）

```
main()
 │
 ├─ parse_args()                # 解析 CLI 参数
 │   支持: -c config -u 手机号 -p 密码 -l 课程ID -s 倍速 -a 策略
 │
 ├─ init_chaoxing()             # 初始化
 │   ├─ Account(phone, pwd)     # 封装账号信息
 │   ├─ Tiku().get_tiku_from_config()  # 动态加载题库类
 │   └─ Chaoxing(account, tiku) # 创建超星 API 实例
 │
 ├─ chaoxing.login()            # 登录（AES 加密密码 → 获取 Cookie）
 │
 ├─ chaoxing.get_course_list()  # 获取课程列表 + 文件夹展开
 │
 ├─ filter_courses()            # 按课程 ID 过滤
 │
 └─ for course in courses:
      └─ process_course(course)
           │
           ├─ get_course_point()          # 获取所有章节
           │
           └─ while index < len(points):  # 遍历章节（支持回滚）
                └─ process_chapter(point)
                     ├─ 已完成的章节 → 跳过（或回滚时强制重处理）
                     ├─ 未开放章节 → handle_not_open_chapter()
                     │    retry:  回滚 index-1（最多 10 次）
                     │    continue: 跳过
                     │    ask:     交互询问
                     │
                     └─ for job in jobs:  # 处理每个任务点
                          ├─ video     → study_video()
                          ├─ document  → study_document()
                          ├─ workid    → study_work()（LLM 答题）
                          └─ read      → strdy_read()
```

### 4.2 视频/音频播放流程（api/base.py）

```
study_video()
 │
 ├─ GET /ananas/status/{objectid}    # 获取视频元信息
 │   └─ 返回: dtoken, duration, crc, key
 │
 └─ while not finished:
      └─ video_progress_log()
           │
           ├─ get_enc()              # MD5 生成加密签名
           │   md5([clazzId][userid][jobid][objectId]
           │        [playingTime*1000][d_yHJ!$pdA~5]
           │        [duration*1000][0_duration])
           │
           └─ GET .../multimedia/log/a/{cpi}/{dtoken}?
                clazzId=&playingTime=&duration=&enc=&rt=&dtype=
                # rt 参数: 尝试 0.9 → 1.0（403 时降级）
                # 返回 200: 继续   返回 403: 降级音频模式
```

### 4.3 章节测验答题流程（api/base.py study_work）

```
study_work()
 │
 ├─ GET /mooc-ans/api/work         # 获取测验题目
 │   └─ with_retry(3次, 指数退避)   # 网络异常重试
 │
 ├─ decode_questions_info()        # 解析题目 HTML
 │
 ├─ for q in questions:            # 逐题处理
 │   │
 │   ├─ Tiku.query(q)              # 题库查答案
 │   │   ├─ CacheDAO.get_cache()   # 先查本地缓存
 │   │   └─ Doubao._query()        # 缓存未命中 → LLM
 │   │        ├─ 统一 34 字符 system prompt（高缓存命中）
 │   │        ├─ POST DeepSeek API（max_tokens=10）
 │   │        └─ 直接返回字母或"对/错"
 │   │
 │   ├─ 答案匹配:                   # LLM 答案 → 选项字母
 │   │   快速路径: 单字母直接取 / 多字母排序
 │   │   退化路径: is_subsequence(LLM输出, 选项文本)
 │   │
 │   └─ 随机兜底: answer == "" → random_answer()
 │
 ├─ 覆盖率判断:
 │   cover_rate >= 80% or rollback → 提交
 │   否则 → 仅保存不提交
 │
 └─ POST /work/addStudentWorkNew   # 提交答题结果
```

### 4.4 章节回滚机制

```
章节锁定时的回滚流程：
  第 1 章(已处理) → 第 2 章(notOpen)
                        │
                  handle_not_open_chapter(retry)
                        │
                  RB.add_times() → __point_index -= 1
                        │
                  回到第 1 章 → has_finished=True 但 rollback_times>0
                        │
                  跳过 has_finished 判断 → 强制重新拉取任务点
                        │
                  重新处理视频/文档/测验 → 提交
                        │
                  __point_index += 1 → 第 2 章（若仍锁定则继续回滚）
                        │
                  同一章节回滚 >10 次 → MaxRollBackExceeded → 跳过该课程
```

---

## 五、题库插件系统

### 5.1 架构设计

`api/answer.py` 使用插件模式管理多种答题后端，所有题库类继承 `Tiku` 基类：

```
Tiku (基类)
 ├─ query(q_info)           # 统一查询接口（含缓存、格式校验）
 ├─ _query(q_info)          # 子类实现：实际查询逻辑
 ├─ _init_tiku()            # 子类实现：初始化配置
 ├─ get_tiku_from_config()  # 工厂方法：provider=类名 → globals()[类名]()
 ├─ judgement_select()      # 判断题答案映射
 └─ get_submit_params()     # 提交策略控制
```

### 5.2 已实现的题库类

| 类名 | 提供者 | API 方式 | config.ini 配置项 |
|------|--------|----------|-------------------|
| `Doubao` | **DeepSeek (v4-flash)** | `requests` 直调，极简 token | `doubao_*` 系列 |
| `AI` | 通用 OpenAI SDK | `openai.OpenAI` 客户端 | `endpoint, key, model` |
| `SiliconFlow` | 硅基流动 | `requests` 直调 | `siliconflow_*` 系列 |
| `TikuYanxi` | 言溪题库 | HTTP GET | `tokens` |
| `TikuLike` | Like 知识库 | HTTP POST | `tokens, likeapi_*` |
| `TikuAdapter` | 通用适配器 | HTTP POST | `url` |

### 5.3 切换题库

修改 `config.ini`：

```ini
[tiku]
provider=Doubao     # 把 Doubao 换成任意已实现的类名
```

当前 `Doubao` 类实际调用的是 DeepSeek API（历史兼容命名），使用 `deepseek-chat` 模型名，DeepSeek 内部路由至 `deepseek-v4-flash`（最低价模型）。

### 5.4 DeepSeek 接口详情（Doubao 类 — 极简模式）

```
POST https://api.deepseek.com/v1/chat/completions
Authorization: Bearer sk-xxx
Content-Type: application/json

{
  "model": "deepseek-chat",
  "temperature": 0,
  "max_tokens": 10,
  "messages": [
    {"role": "system", "content": "你是答题器。只输出答案，禁止解释。单选→字母(A/B/C/D)，多选→连续字母(ABD)，判断→对/错，填空→文本。"},
    {"role": "user",   "content": "题干...\nA 选项1\nB 选项2\n..."}
  ]
}
```

**成本优化要点：**
- 统一 34 字符 system prompt（所有题型共用），DeepSeek 前缀缓存命中率近 100%
- `temperature: 0` 确定性输出，零采样开销
- `max_tokens: 10` 严格限制输出（答案仅需 1-3 字符）
- 模型直接返回字母或"对/错"，不输出 JSON 格式，不输出解释文本
- 相比旧版（max_tokens=1024、5 种 prompt、JSON 完整文本输出），单次调用 token 消耗从 ~2500 降至 ~5

### 5.5 答案匹配算法（双路径）

**快速路径（优先）：** LLM 直接输出字母时零开销匹配
```python
# 单选: "B" → answer = "B"
if q["type"] == "single" and len(res) == 1 and res.isalpha():
    answer = res.upper()
# 多选: "ABD" → answer = "ABD"（已排序）
elif q["type"] == "multiple" and re.fullmatch(r'[A-Fa-f]{2,}', res):
    answer = "".join(sorted(res.upper()))
# 判断: "对" → answer = "true"
elif q["type"] == "judgement" and res in ("对", "错"):
    answer = "true" if res == "对" else "false"
```

**退化路径（兜底）：** LLM 输出文本时走子序列匹配
```python
# clean_res: 去除答案文本的前缀字母和标点符号
# is_subsequence(a, o): 判断答案 a 是否为选项 o 的子序列
for option in ["美国选择", "中国选择", "日本选择", "瑞士选择"]:
    if is_subsequence("瑞士", option):  # True
        answer = "D"
```

---

## 六、配置说明

### 6.1 config.ini

```ini
[common]
username =                    # 学习通手机号（本地运行时填写）
password =                    # 登录密码（本地运行时填写）
course_list =                 # 课程ID，逗号分隔（如 264391804）
speed = 1                     # 视频倍速（默认1，最大2）

[tiku]
provider = Doubao             # 题库提供者类名
submit = true                 # true=提交 false=仅保存
cover_rate = 0.8              # 题库覆盖率阈值（低于此值不提交）
true_list = 正确,对,√,是       # 判断题正确关键词
false_list = 错误,错,×,否      # 判断题错误关键词

# DeepSeek 配置（provider=Doubao 时生效）
doubao_endpoint = https://api.deepseek.com/v1/chat/completions
doubao_api_key = sk-xxx       # DeepSeek API Key
doubao_model = deepseek-chat  # deepseek-chat | deepseek-reasoner
doubao_min_interval = 1       # 请求间隔（秒）
```

### 6.2 CLI 参数

```
-c, --config         配置文件路径
-u, --username       学习通手机号
-p, --password       登录密码
-l, --list           课程 ID 列表（逗号分隔）
-s, --speed          视频倍速（默认 1，最大 2）
-a, --notopen-action 未开放章节处理策略：retry | ask | continue
-v, --verbose        调试模式
```

---

## 七、GitHub Actions 部署

### 7.1 工作流配置

```yaml
# .github/workflows/main.yml
name: 学习通自动刷课

on:
  push:
    branches: [main]
  schedule:
    - cron: "0 0 * * *"       # 每天 UTC 0:00（北京时间 8:00）
  workflow_dispatch:           # 手动触发

jobs:
  run:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./SuperStar-main  # 仓库嵌套结构
    steps:
      - checkout → setup-python 3.10 → pip install
      - 动态生成 config.ini（API Key 从 secrets 注入）
      - python main.py -u "手机号" -p "密码" -l "课程ID" -a retry
```

### 7.2 修改账号/密码/课程

直接编辑 `.github/workflows/main.yml` 第 49 行：

```yaml
run: python main.py -u "你的手机号" -p "你的密码" -l "课程ID" -a retry
```

### 7.3 GitHub Secrets

仅需配置 1 个 Secret：

| Name | 值 |
|------|-----|
| `DEEPSEEK_API_KEY` | DeepSeek API Key（`sk-` 开头） |

账号密码已在 workflow 中写死，无需额外配置 Secret。

---

## 八、数据流图

```
用户 ──CLI/CI──→ main.py
                  │
                  ├──→ config.ini / CLI args ──→ (username, password, course_list)
                  │
                  ├──→ POST passport2.chaoxing.com/fanyalogin
                  │    └── AES(password) → Cookie
                  │
                  ├──→ GET mooc2-ans.chaoxing.com → 课程列表
                  │
                  └──→ foreach course:
                         │
                         GET mooc2-ans → 章节列表
                         │
                         foreach chapter:
                           │
                           GET mooc1/knowledge/cards → 任务点列表
                           │
                           foreach job:
                             ├── [video]  ──→ GET /ananas/status → MD5 enc → GET /multimedia/log
                             ├── [doc]    ──→ GET /ananas/job/document
                             ├── [read]   ──→ GET /ananas/job/readv2
                             └── [workid] ──→ GET /api/work → Tiku.query()
                                              │
                                              ├── CacheDAO (本地缓存)
                                              └── DeepSeek API (LLM 答题)
                                                  │
                                                  └── POST /work/addStudentWorkNew
```

---

## 九、缓存机制

`CacheDAO` 以题目标题为 key，答案为 value，存储在 `cache.json` 中。同一题目多次遇到时直接返回缓存答案，无需重复调用 LLM。缓存文件在 `.gitignore` 中，不会提交到仓库。

---

## 十、异常处理

| 异常 | 处理方式 |
|------|----------|
| `LoginError` | 登录失败，退出程序 |
| `InputFormatError` | 缺少凭证或输入格式错误 |
| `MaxRollBackExceeded` | 回滚超限，跳过当前课程 |
| `MaxRetryExceeded` | 网络请求重试超限 |
| 视频 403 | 降级音频模式 → 仍失败则跳过任务点 |
| LLM 返回异常 | 记录日志，回退到随机答案 |
| 网络超时 | 指数退避重试（30 秒超时） |

---

## 十一、安全注意事项

1. `config.ini` 包含 API Key，已在 `.gitignore` 中，不要提交到仓库
2. GitHub Actions 中 `config.ini` 动态生成，API Key 通过 Secret 注入
3. 登录密码经 AES-CBC 加密后传输，超星的加密 key 在 `api/config.py` 中硬编码
4. Cookie 存储在本地 `cookies.txt`，有效期约 30 分钟
